const conta = Number(prompt('Valor da conta: R$'))
const clientes = Number(prompt('Número de clientes: '))
const valorPorClientes = conta / clientes

alert(`Valor por clientes R$${valorPorClientes}`)
